//! Ponto de entrada do núcleo do PawnPro.
//!
//! A lógica está na biblioteca (`lib.rs`); aqui fica só o que precisa de um
//! processo de verdade. Ver `docs/architecture.md`.

fn main() {
    // O supervisor ainda não existe: a migração começou pelos módulos que a
    // extensão mais precisava tirar do TypeScript. Ver `docs/supervision.md`
    // para a ordem prevista.
}
